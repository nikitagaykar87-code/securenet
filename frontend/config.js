const CONFIG = {
    // 🚀 AUTOMATIC CONFIGURATION FOR DEPLOYMENT
    // This will automatically point to your backend URL wherever you host it.
    API_BASE_URL: window.location.origin,
    FRONTEND_URL: window.location.origin
};
